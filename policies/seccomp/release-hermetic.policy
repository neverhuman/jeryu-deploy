name = "release-hermetic-phase4-seccomp"
default_action = "kill-process"
allow_groups = [
  "process-basic",
  "file-readwrite-workspace",
  "futex",
  "time",
  "rust-build-tooling"
]
deny = ["network-syscalls", "keyctl", "bpf", "perf_event_open", "ptrace", "mount", "umount2"]
